export interface PizzaMagicShop {
  name: string;
  postcode: string;
  address: string;
  latitude: number;
  longitude: number;
}


export interface ShopLocation {
  name: string;
  postcode: string;
  address: string;
  distance: number;
}
